# Pinned to recent stable versions as of 2026-05.
# OpenAI deps added in Sprint 2.

fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Sprint 1: crypto + SSE + ULID session ids
cryptography==44.0.0
sse-starlette==2.2.1
python-ulid==3.0.0

# Sprint 2: real LLM pipeline (Whisper + GPT-4o + TTS)
openai==1.58.1

# Test deps (used by `pytest`, not by the running server)
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

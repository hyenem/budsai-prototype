# Pinned to recent stable versions as of 2026-05.
# Sprint 0 deps only; OpenAI/Opus/etc added in later sprints.

fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Test deps (used by `pytest`, not by the running server)
pytest==8.3.4
httpx==0.28.1
